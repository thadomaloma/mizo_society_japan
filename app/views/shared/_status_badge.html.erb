<span class="inline-flex max-w-full items-center truncate rounded-full px-2 py-0.5 text-[11px] font-black capitalize leading-5 ring-1 ring-inset sm:px-2.5 sm:py-1 sm:text-xs <%= status_badge_classes(status) %>" title="<%= status.to_s.humanize %>">
  <%= status_badge_label(status, compact: local_assigns.fetch(:compact, false)) %>
</span>
