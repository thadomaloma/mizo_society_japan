<span class="inline-flex max-w-full items-center truncate rounded-md px-2 py-0.5 text-[10px] font-extrabold uppercase leading-5 tracking-[0.04em] ring-1 ring-inset sm:text-[11px] <%= status_badge_classes(status) %>" title="<%= status.to_s.humanize %>">
  <%= status_badge_label(status, compact: local_assigns.fetch(:compact, false)) %>
</span>
