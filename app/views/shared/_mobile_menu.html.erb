<div data-mobile-menu-target="drawer" data-action="click->mobile-menu#closeFromBackdrop" class="fixed inset-0 z-50 hidden bg-slate-950/60 backdrop-blur-[2px] lg:hidden">
  <aside data-mobile-menu-target="panel" class="flex h-full w-[min(19rem,84vw)] -translate-x-full flex-col bg-[#172033] text-white shadow-2xl ring-1 ring-white/10 transition-transform duration-150 ease-out dark:bg-[#020617]">
    <div class="flex items-center justify-between border-b border-white/[0.08] px-4 pb-3 pt-[max(0.9rem,env(safe-area-inset-top))]">
      <%= link_to(dashboard_home_path, class: "flex min-w-0 items-center gap-2.5", data: { action: "click->mobile-menu#close" }) do %>
        <span class="grid h-9 w-9 shrink-0 place-items-center overflow-hidden rounded-lg bg-white ring-1 ring-white/20">
          <%= image_tag "/icons/msj-portal-192x192-20260709.png", alt: "MSJ", class: "h-9 w-9 object-cover" %>
        </span>
        <span class="min-w-0">
          <span class="block truncate text-[13px] font-black tracking-tight text-[#F8FAFC]">Mizo Society of Japan</span>
          <span class="mt-0.5 block text-[11px] font-semibold tracking-[0.14em] text-slate-300">Tlawmngai Pawl</span>
        </span>
      <% end %>
      <button type="button" data-action="mobile-menu#close" class="grid h-8 w-8 place-items-center rounded-lg bg-white/10 text-slate-100 transition hover:bg-white/15 active:scale-95" aria-label="Close menu">
        <%= icon_svg(:x_mark, classes: "h-4 w-4") %>
      </button>
    </div>

    <% mobile_menu_items = admin_portal_user? ? admin_nav_items + portal_nav_items.select { |item| item[:label] == "Profile" } : portal_nav_items %>
    <nav class="flex-1 space-y-1 overflow-y-auto px-3 py-4" aria-label="Mobile menu">
      <% payment_labels = [ "Payments", "Payment Records", "Payment Plans" ] %>
      <% payment_items = mobile_menu_items.select { |item| item[:label].in?(payment_labels) } %>
      <% mobile_menu_items.each do |item| %>
        <% if admin_portal_user? && item[:label] == "Payments" %>
          <%= render "shared/payment_nav_group", items: payment_items, mobile: true %>
          <% next %>
        <% end %>
        <% next if admin_portal_user? && item[:label].in?(payment_labels) %>
        <% next if item[:label] == "Settings" && !current_user.super_admin? %>
        <% accessible = !admin_portal_user? || item[:label] == "Profile" || nav_item_visible?(item) %>
        <% active = nav_active?(item) || (item[:label] == "Settings" && settings_section_active?) %>
        <% if accessible %>
          <%= link_to item[:path], aria: { current: ("page" if active) }, data: { action: "click->mobile-menu#close" }, class: "flex min-h-11 items-center gap-3 rounded-lg px-3 py-2.5 text-sm font-extrabold transition #{active ? "bg-red-600 text-white shadow-sm shadow-red-950/20" : "text-slate-300 hover:bg-white/[0.07] hover:text-white"}" do %>
            <%= icon_svg(item[:icon], classes: "h-[18px] w-[18px]") %>
            <span><%= item[:label] %></span>
          <% end %>
        <% else %>
          <span class="flex min-h-11 cursor-not-allowed items-center gap-3 rounded-lg px-3 py-2.5 text-sm font-bold text-slate-600">
            <%= icon_svg(item[:icon], classes: "h-4 w-4") %>
            <span class="flex-1"><%= item[:label] %></span>
            <%= icon_svg(:lock, classes: "h-3.5 w-3.5") %>
          </span>
        <% end %>
      <% end %>
    </nav>

    <div class="border-t border-white/10 p-2.5 pb-[calc(env(safe-area-inset-bottom)+0.65rem)]">
      <%= button_to destroy_user_session_path, method: :delete, class: "flex w-full items-center gap-2.5 rounded-xl px-3 py-2.5 text-[13.5px] font-black leading-6 text-red-300 transition hover:bg-red-500/10" do %>
        <%= icon_svg(:logout, classes: "h-4 w-4") %>
        <span>Log out</span>
      <% end %>
    </div>
  </aside>
</div>
