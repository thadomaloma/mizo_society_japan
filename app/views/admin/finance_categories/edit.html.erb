<% content_for :page_title, "Edit Finance Category" %>

<div class="space-y-6">
  <%= render "shared/page_header", eyebrow: "Finance", title: "Edit Finance Category", subtitle: @finance_category.name %>
  <%= render "form", finance_category: @finance_category %>
</div>
